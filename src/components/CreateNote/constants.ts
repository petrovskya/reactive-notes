import { EMPTY_STRING } from 'config';

import { ICreateNoteFormValues } from './types';

export const CREATE_NOTE_INITIAL_VALUES: ICreateNoteFormValues = {
  title: EMPTY_STRING,
  description: EMPTY_STRING,
};
