import styled from '@emotion/styled';
import { Box } from '@mui/material';

export const StyledNav = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  gap: '40px',
  width: '100%',
  fontFamily: 'inherit',
});
